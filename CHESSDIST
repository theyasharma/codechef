#include <bits/stdc++.h>
using namespace std;

int main() 
{
    int t;
    cin>>t;
    while(t--){
      int x1,y1,x2,y2,d,x,y;
      cin>>x1>>y1>>x2>>y2;
      x=abs(x2-x1);
      y=abs(y2-y1);
      cout<<max(x,y)<<endl;
     
    }
}
