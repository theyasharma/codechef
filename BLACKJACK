#include <bits/stdc++.h>
using namespace std;

int main() 
{
    int t;
    cin>>t;
    while(t--){
      int a,b,sum;
      cin>>a>>b;
      sum=(21-(a+b));
      if(sum>10){
        cout<<-1<<endl;
      }else{
        cout<<sum<<endl;
      }
    }
}
